---
declared-services:
  mean-mongo:
    label: mongodb
    plan: 100
applications:
- name: mean
  host: mean-${random-word}
  memory: 128M
  disk_quota: 1024M
  domain: eu-gb.mybluemix.net
  instances: 1
  services:
  - mean-mongo
  env:
    NODE_ENV: cloud-foundry
